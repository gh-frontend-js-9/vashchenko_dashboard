export interface projectsInterfaceProps {
  title?: string,
  company?: object,
  cost?: object,
  deadline?: object,
  assigned?: object,
  axiosAllProjectsGet?: any,
  allProjects?: any,
  loads?: boolean,
  logIn?: boolean,
  logInError?: boolean,
  logOut?: boolean,
}
